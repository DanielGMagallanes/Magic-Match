import { ColletionViewModel } from './colletion-view-model';

describe('ColletionViewModel', () => {
  it('should create an instance', () => {
    expect(new ColletionViewModel()).toBeTruthy();
  });
});
